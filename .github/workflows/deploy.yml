name: Deploy Action

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: Install & Build
      - uses: creepinson/pnpm-action@master
        with:
        arguments: '["install", "build"]'

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: ./dist
